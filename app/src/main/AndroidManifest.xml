<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".IMDBApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IMDb"
        tools:targetApi="33">

        <activity
            android:name="ro.vlad.theaters_presentation.TheatersActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity
            android:name="ro.vlad.top_presentation.TopActivity"
            android:exported="true" />

        <activity
            android:name="ro.vlad.popular_presentation.PopularActivity"
            android:exported="true" />

        <activity
            android:name="ro.vlad.details_presentation.DetailsActivity"
            android:exported="true" />

        <activity android:name="ro.vlad.search_presentation.SearchActivity"
            android:exported="true" />

    </application>

</manifest>